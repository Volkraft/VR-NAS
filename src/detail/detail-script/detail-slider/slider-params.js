import styles from './slider.module.css'
const sliderParams = {
    tagName: 'div',
    styles: styles.slider,
}

const wrapperHiddenParams = {
    tagName: 'div',
    styles: styles.wrapperHidden,
}

const sliderTrackParams = {
    tagName: 'div',
    styles: styles.sliderTrack,
}

const slideParams = {
    tagName: 'div',
    styles: styles.slide,
}

const slideTitleParams = {
    tagName: 'h6',
    styles: styles.slideTitle,
}

const slideSubtitleParams = {
    tagName: 'p',
    styles: styles.slideSubtitle,
}

const slideDescriptionParams = {
    tagName: 'p',
    styles: styles.slideDescription,
}

const slideArticleParams = {
    tagName: 'article',
    styles: styles.slideArticle,
}

const slideArticleTitleParams = {
    tagName: 'h3',
    styles: styles.slideArticleTitle,
}

const slideArticleListParams = {
    tagName: 'ul',
    styles: styles.slideArticleList,
}

const slideArticleListItemParams = {
    tagName: 'li',
    styles: styles.slideArticleListItem,
}

const slideBtnParams = {
    tagName: 'a',
    styles: styles.slideBtn,
}

const slideBtnTitleParams = {
    tagName: 'span',
    styles: styles.slideBtnTitle,
}

const listPaginationParams = {
    tagName: 'ul',
    styles: styles.listPagination,
}
const paginationBtnParams = {
    tagName: 'li',
    styles: styles.paginationBtn,
}

const activeBtnParams = {
    tagName: 'li',
    styles: styles.paginationBtnActive,
}

const wrapperArrowsParams = {
    tagName: 'div',
    styles: styles.wrapperArrows,
}

const arrowLeftParams = {
    tagName: 'button',
    styles: styles.arrowLeft,
}

const arrowRightParams = {
    tagName: 'button',
    styles: styles.arrowRight,
}

export {
    sliderParams,
    wrapperHiddenParams,
    sliderTrackParams,
    slideParams,
    slideTitleParams,
    slideSubtitleParams,
    slideDescriptionParams,
    slideArticleParams,
    slideArticleTitleParams,
    slideArticleListParams,
    slideArticleListItemParams,
    slideBtnParams,
    slideBtnTitleParams,
    listPaginationParams,
    paginationBtnParams,
    activeBtnParams,
    wrapperArrowsParams,
    arrowLeftParams,
    arrowRightParams,
}
